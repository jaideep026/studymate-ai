# Only needed for local dev (frontend on :5173, backend on :5000).
# In production this is unset on purpose: the frontend is served by the
# same Express server as the API, so it just calls a relative /api path.
VITE_API_BASE_URL=http://localhost:5000/api
